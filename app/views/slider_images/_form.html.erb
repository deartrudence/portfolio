<%= form_for(@slider_image) do |f| %>
  <% if @slider_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider_image.errors.count, "error") %> prohibited this slider_image from being saved:</h2>

      <ul>
      <% @slider_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group u-spacer20">
    <%= f.label :slider_order %><br>
    <%= f.number_field :slider_order, class: 'input' %>
  </div>
  <div class="form-group u-spacer20">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'input' %>
  </div>
  <div class="form-group u-spacer20">
    <%= f.label "Upload Slider Photo" %><br>
    <%=f.file_field :photo %>
  </div>
  <div class="actions">
    <%= f.submit 'Add Slider Photo', class: 'button' %>
  </div>
<% end %>
